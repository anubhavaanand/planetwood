import * as zustand from 'zustand';
import { G as GamePhase, S as SceneStoreState } from './scene-Dl2UZ826.mjs';
import 'react';
import '@react-three/drei';
import '@react-three/fiber';
import 'three';

interface GameStoreState {
    /** Current game phase */
    phase: GamePhase;
    /** Total elapsed time in seconds (only increments while playing) */
    elapsed: number;
    /** Number of frames rendered while playing */
    frameCount: number;
    setPhase: (phase: GamePhase) => void;
    /** Internal — called by GameLoopTick once per frame. Do not call manually. */
    _tick: (delta: number) => void;
    pause: () => void;
    resume: () => void;
    reset: () => void;
}
declare const useGameStore: zustand.UseBoundStore<zustand.StoreApi<GameStoreState>>;

declare const useSceneStore: zustand.UseBoundStore<zustand.StoreApi<SceneStoreState>>;

export { type GameStoreState, SceneStoreState, useGameStore, useSceneStore };
