export { I as AABBColliderDef, J as ActionMap, e as ActorEventProps, f as ActorPhysicsProps, d as ActorTransformProps, A as AmbientLightProps, i as CameraControlsType, j as CameraFollowConfig, C as CameraProps2D, b as CameraProps3D, h as CameraType, K as CircleColliderDef, L as ColliderDef, N as ColliderShape, Q as CollisionCallback, R as CollisionEvent, D as DirectionalLightProps, E as EnvironmentProps, p as GameLoopCallback, T as GameLoopStage, G as GamePhase, V as GridCellCallback, X as GridCollisionConfig, Y as KeyState, M as MapControlsProps, O as OrbitControlsProps, l as OrthographicCameraProps, k as PerspectiveCameraProps, a2 as PhysicsColliderType, m as PointerLockControlsProps, Z as PointerState, g as PrimitiveMaterialType, P as PrimitiveShape, a3 as RigidBodyType, n as SceneConfig, a4 as SceneEntry, F as SceneLifecycleCallbacks, _ as SceneStatus, S as SceneStoreState, a as SkyProps, $ as SphereColliderDef, a0 as TransitionConfig, a5 as TransitionState, a1 as TransitionType, o as UseCameraOptions, U as UseCameraReturn, u as UseCollisionOptions, v as UseCollisionReturn, q as UseGameLoopOptions, r as UseGameLoopReturn, w as UseGridCollisionOptions, x as UseGridCollisionReturn, s as UseInputOptions, t as UseInputReturn, y as UsePhysicsReturn, B as UseSceneDataReturn, z as UseSceneReturn, H as UseSceneTransitionReturn, W as WorldMode, c as WorldPhysicsConfig } from './scene-Dl2UZ826.mjs';
export { d as ActorRef, b as AssetEntry, h as AssetGroupConfig, c as AssetLoadError, a as AssetManifest, i as AssetType, j as AudioChannel, k as AudioFormat, A as AudioListenerConfig, l as AudioSpriteMap, m as AudioSpriteRegion, B as BurstConfig, Z as CacheEntry, C as ColorGradient, n as ColorGradientStop, o as ColorRange, p as ConeShapeConfig, q as CrossfadeOptions, r as CurveKeyframe, D as DistanceModel, E as EdgeShapeConfig, s as EmissionMode, t as EmitterShape, u as EmitterShapeConfig, v as LifetimeCurve, L as LoadingProgress, M as MusicOptions, N as NetworkedConfig, O as OverLifetimeConfig, w as PanningModel, x as ParticleBlendMode, _ as ParticleData, y as ParticleEmitterConfig, P as ParticleEmitterProps, z as ParticlePreset, F as ParticlePropertyConfig, G as ParticleSpace, H as PlaySoundOptions, I as PointShapeConfig, R as RectangleShapeConfig, J as RingShapeConfig, S as SoundDefinition, K as SoundHandle, Q as SoundMap, T as SoundState, V as SpatialAudioOptions, W as SphereShapeConfig, X as SpriteSheetConfig, U as UseAudioOptions, e as UseAudioReturn, f as UseParticlesOptions, g as UseParticlesReturn, Y as ValueRange } from './particles-3P2V9bLu.mjs';
export { Group, Object3D } from 'three';
import 'react';
import '@react-three/drei';
import '@react-three/fiber';

/** A normalized easing function: takes t in [0,1], returns eased value */
type EasingFn = (t: number) => number;
/** Named easing preset identifier */
type EasingPreset = "linear" | "quad.in" | "quad.out" | "quad.inOut" | "cubic.in" | "cubic.out" | "cubic.inOut" | "quart.in" | "quart.out" | "quart.inOut" | "quint.in" | "quint.out" | "quint.inOut" | "sine.in" | "sine.out" | "sine.inOut" | "expo.in" | "expo.out" | "expo.inOut" | "circ.in" | "circ.out" | "circ.inOut" | "back.in" | "back.out" | "back.inOut" | "elastic.in" | "elastic.out" | "elastic.inOut" | "bounce.in" | "bounce.out" | "bounce.inOut" | "spring";
/** Easing can be a preset name, a custom function, or a cubic-bezier tuple [x1,y1,x2,y2] */
type EasingInput = EasingPreset | EasingFn | [number, number, number, number];
type TweenState = "idle" | "pending" | "delayed" | "playing" | "completed" | "killed";
type TweenDirection = "forward" | "reverse";
interface TweenConfig<T extends object = Record<string, unknown>> {
    /** The object whose properties will be animated */
    target: T;
    /** Properties to animate to (end values). Supports dot-notation: "position.x" */
    to: Partial<Record<string, number>>;
    /** Explicit start values. If omitted, current values are captured at creation. */
    from?: Partial<Record<string, number>>;
    /** Duration in seconds. Default: 0.3 */
    duration?: number;
    /** Easing function or preset name. Default: "quad.out" */
    ease?: EasingInput;
    /** Delay before starting, in seconds. Default: 0 */
    delay?: number;
    /** Number of additional repeats. -1 for infinite. Default: 0 */
    repeat?: number;
    /** Delay between repeats, in seconds. Default: 0 */
    repeatDelay?: number;
    /** Reverse direction on each repeat. Default: false */
    yoyo?: boolean;
    /** Playback speed multiplier. Default: 1 */
    speed?: number;
    /** Named group for batch control. Default: "" */
    group?: string;
    /** Keep tween alive after completion (not auto-released). Default: false */
    persist?: boolean;
    /** Continue updating when game is paused. Default: false */
    ignorePause?: boolean;
    onStart?: () => void;
    onUpdate?: (progress: number) => void;
    onComplete?: () => void;
    onRepeat?: (count: number) => void;
    onYoyo?: (direction: TweenDirection) => void;
    onKill?: () => void;
}
/** Configuration for a target-less number tween */
interface NumberTweenConfig {
    from: number;
    to: number;
    duration?: number;
    ease?: EasingInput;
    delay?: number;
    repeat?: number;
    repeatDelay?: number;
    yoyo?: boolean;
    speed?: number;
    group?: string;
    ignorePause?: boolean;
    onStart?: () => void;
    onUpdate?: (value: number, progress: number) => void;
    onComplete?: () => void;
    onRepeat?: (count: number) => void;
    onYoyo?: (direction: TweenDirection) => void;
    onKill?: () => void;
}
interface TweenControls {
    readonly id: number;
    readonly state: TweenState;
    readonly progress: number;
    pause: () => TweenControls;
    resume: () => TweenControls;
    kill: () => TweenControls;
    restart: () => TweenControls;
    seek: (progress: number) => TweenControls;
    setSpeed: (multiplier: number) => TweenControls;
    chain: (config: TweenConfig) => TweenControls;
    readonly finished: Promise<void>;
}
interface TweenGroupControls {
    pause: () => void;
    resume: () => void;
    kill: () => void;
    readonly count: number;
}
/**
 * Timeline position specifier.
 * - number: absolute time in seconds
 * - "+=0.5": relative to end of previous entry
 * - "-=0.2": overlap with previous entry
 * - "label": at a named label
 * - "label+=0.3": offset from label
 * - "<": start of previous entry (parallel)
 * - "<+=0.5": offset from start of previous entry
 */
type TimelinePosition = number | string;
interface TimelineConfig {
    /** Number of additional repeats. -1 for infinite. Default: 0 */
    repeat?: number;
    /** Delay between repeats, in seconds. Default: 0 */
    repeatDelay?: number;
    /** Reverse direction on each repeat. Default: false */
    yoyo?: boolean;
    /** Playback speed multiplier. Default: 1 */
    speed?: number;
    /** Start paused. Default: false */
    paused?: boolean;
    /** Continue updating when game is paused. Default: false */
    ignorePause?: boolean;
    /** Named group for batch control */
    group?: string;
    onStart?: () => void;
    onUpdate?: (progress: number) => void;
    onComplete?: () => void;
    onRepeat?: (count: number) => void;
}
interface TimelineControls {
    readonly id: number;
    readonly state: TweenState;
    readonly progress: number;
    readonly duration: number;
    add: <T extends object>(config: TweenConfig<T>, position?: TimelinePosition) => TimelineControls;
    addCallback: (callback: () => void, position?: TimelinePosition) => TimelineControls;
    addLabel: (name: string, position?: TimelinePosition) => TimelineControls;
    play: () => TimelineControls;
    pause: () => TimelineControls;
    resume: () => TimelineControls;
    kill: () => TimelineControls;
    restart: () => TimelineControls;
    seek: (timeOrLabel: number | string) => TimelineControls;
    setSpeed: (multiplier: number) => TimelineControls;
    readonly finished: Promise<void>;
}
interface UseTweenReturn {
    /** Create and start a property tween */
    tween: <T extends object>(config: TweenConfig<T>) => TweenControls;
    /** Create and start a target-less number tween */
    tweenNumber: (config: NumberTweenConfig) => TweenControls;
    /** Create a timeline for sequenced animations */
    timeline: (config?: TimelineConfig) => TimelineControls;
    /** Kill all tweens created by this hook instance */
    killAll: () => void;
    /** Pause all tweens created by this hook instance */
    pauseAll: () => void;
    /** Resume all tweens created by this hook instance */
    resumeAll: () => void;
}

export type { EasingFn, EasingInput, EasingPreset, NumberTweenConfig, TimelineConfig, TimelineControls, TimelinePosition, TweenConfig, TweenControls, TweenDirection, TweenGroupControls, TweenState, UseTweenReturn };
