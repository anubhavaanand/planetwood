import { Object3D, ColorRepresentation, Texture } from 'three';
import { RefObject } from 'react';
import { Vector3, Euler } from '@react-three/fiber';

/** Reference to a registered Actor for networking purposes */
interface ActorRef {
    id: string;
    object3D: Object3D;
    rigidBody?: any;
    userData: Record<string, unknown>;
}
/** Configuration for networked Actor sync behavior */
interface NetworkedConfig {
    /** What to synchronize. Default: 'transform' */
    sync?: 'transform' | 'physics' | false;
    /** PeerId of the owning player. Undefined = host-owned */
    owner?: string;
    /** Custom properties to sync across the network */
    custom?: Record<string, unknown>;
    /** Per-property interpolation toggle. Keys match custom property names */
    interpolate?: Record<string, boolean>;
}

/** Volume channel names. "master" controls the global output. */
type AudioChannel = "master" | "sfx" | "music" | "ui" | "ambient" | "voice";
/** Supported audio formats for codec negotiation */
type AudioFormat = "mp3" | "ogg" | "wav" | "webm" | "aac" | "flac";
/** Playback state of a single sound instance */
type SoundState = "playing" | "paused" | "stopped";
/** Defines a named region within an audio file */
interface AudioSpriteRegion {
    /** Start offset in seconds */
    start: number;
    /** Duration in seconds */
    duration: number;
    /** Whether this region should loop. Default: false */
    loop?: boolean;
}
/** Map of sprite name to region definition */
type AudioSpriteMap = Record<string, AudioSpriteRegion>;
/** Configuration for registering a sound with the AudioManager */
interface SoundDefinition {
    /**
     * URL(s) of the audio file.
     * - Single string: used as-is
     * - Array: format auto-detection picks the first supported format
     */
    src: string | string[];
    /** Volume channel this sound belongs to. Default: "sfx" */
    channel?: AudioChannel;
    /** Base volume (0-1). Default: 1 */
    volume?: number;
    /** Playback rate (0.5 = half speed, 2 = double speed). Default: 1 */
    rate?: number;
    /** Whether to loop. Default: false */
    loop?: boolean;
    /** Maximum simultaneous instances. Default: 5 */
    maxInstances?: number;
    /** Whether to preload on registration. Default: true */
    preload?: boolean;
    /** Audio sprite regions, if this is a sprite sheet */
    sprites?: AudioSpriteMap;
    /** Minimum seconds between play calls. Requests within cooldown are dropped. */
    cooldown?: number;
}
/** Panning model for spatial audio */
type PanningModel = "HRTF" | "equalpower";
/** Distance model for spatial attenuation */
type DistanceModel = "linear" | "inverse" | "exponential";
/** Configuration for 3D spatial audio on a sound instance */
interface SpatialAudioOptions {
    /** Ref to the Three.js Object3D emitting the sound */
    ref: RefObject<Object3D | null>;
    /** Panning model. Default: "HRTF" */
    panningModel?: PanningModel;
    /** Distance model for volume rolloff. Default: "inverse" */
    distanceModel?: DistanceModel;
    /** Distance at which volume starts to decrease. Default: 1 */
    refDistance?: number;
    /** Maximum distance at which sound is still audible. Default: 100 */
    maxDistance?: number;
    /** How quickly volume decreases with distance. Default: 1 */
    rolloffFactor?: number;
    /** Inner cone angle in degrees (full volume). Default: 360 */
    coneInnerAngle?: number;
    /** Outer cone angle in degrees. Default: 360 */
    coneOuterAngle?: number;
    /** Volume outside the outer cone (0-1). Default: 0 */
    coneOuterGain?: number;
    /** Update position every frame from the Object3D ref. Default: true */
    trackPosition?: boolean;
}
/** Options passed when playing a sound */
interface PlaySoundOptions {
    /** Override volume for this instance (0-1) */
    volume?: number;
    /** Override playback rate */
    rate?: number;
    /** Override loop setting */
    loop?: boolean;
    /** If this sound has sprites, play this named sprite */
    sprite?: string;
    /** Spatial audio configuration for 3D positioning */
    spatial?: SpatialAudioOptions;
    /** Fade in duration in seconds. Default: 0 */
    fadeIn?: number;
    /** Delay before playback starts, in seconds. Default: 0 */
    delay?: number;
    /** Callback when playback finishes (not called if looping) */
    onEnd?: () => void;
    /** Callback if playback fails */
    onError?: (error: Error) => void;
}
/** Handle to a playing sound instance, returned by play() */
interface SoundHandle {
    /** Unique instance ID */
    readonly id: number;
    /** Current state */
    readonly state: SoundState;
    /** Stop and release this instance */
    stop: () => void;
    /** Pause this instance */
    pause: () => void;
    /** Resume this instance */
    resume: () => void;
    /** Fade volume over duration (seconds) */
    fade: (from: number, to: number, duration: number) => void;
    /** Set volume (0-1) for this instance */
    setVolume: (volume: number) => void;
    /** Set playback rate */
    setRate: (rate: number) => void;
}
/** Configuration for music crossfading */
interface CrossfadeOptions {
    /** Duration of the crossfade in seconds. Default: 2 */
    duration?: number;
}
/** Options for the music system */
interface MusicOptions {
    /** Volume (0-1). Default: 1 */
    volume?: number;
    /** Whether to loop the track. Default: true */
    loop?: boolean;
    /** Crossfade settings when switching tracks */
    crossfade?: CrossfadeOptions;
    /** If using sprites from a registered sound, the sprite name */
    sprite?: string;
}
/** Sounds map passed to useAudio for registration */
type SoundMap = Record<string, SoundDefinition>;
/** Options for the useAudio hook */
interface UseAudioOptions {
    /** Map of sound names to definitions. Registered on mount, unregistered on unmount. */
    sounds?: SoundMap;
    /** Whether audio from this hook is active. Default: true */
    enabled?: boolean;
}
/** Return value of the useAudio hook */
interface UseAudioReturn {
    /** Play a registered sound by name */
    play: (name: string, options?: PlaySoundOptions) => SoundHandle | null;
    /** Stop all instances of a named sound */
    stop: (name: string) => void;
    /** Pause all instances of a named sound */
    pause: (name: string) => void;
    /** Resume all paused instances of a named sound */
    resume: (name: string) => void;
    /** Check if any instance of a named sound is currently playing */
    isPlaying: (name: string) => boolean;
    /** Play a music track (crossfades from current if one is playing) */
    playMusic: (src: string | string[], options?: MusicOptions) => void;
    /** Stop the current music track */
    stopMusic: (fadeOut?: number) => void;
    /** Pause the music */
    pauseMusic: () => void;
    /** Resume the music */
    resumeMusic: () => void;
    /** Check if music is currently playing */
    isMusicPlaying: () => boolean;
    /** Set volume for a channel (0-1) */
    setVolume: (channel: AudioChannel, volume: number) => void;
    /** Get current volume for a channel */
    getVolume: (channel: AudioChannel) => number;
    /** Mute/unmute a channel */
    setMute: (channel: AudioChannel, muted: boolean) => void;
    /** Check if a channel is muted */
    isMuted: (channel: AudioChannel) => boolean;
    /** Set the master mute */
    setMasterMute: (muted: boolean) => void;
    /** Preload a sound by name */
    preload: (name: string) => Promise<void>;
    /** Preload multiple sounds */
    preloadAll: (names: string[]) => Promise<void>;
    /** Whether the AudioContext has been unlocked */
    isUnlocked: boolean;
    /** Whether the audio system is ready */
    isReady: boolean;
}
/** Configuration for the AudioListener component */
interface AudioListenerConfig {
    /** Override the listener position source. Defaults to the active R3F camera. */
    listenerRef?: RefObject<Object3D | null>;
}

/** Supported asset format categories */
type AssetType = "gltf" | "texture" | "audio" | "json" | "binary";
/** Single asset entry in a manifest */
interface AssetEntry {
    /** Unique key for referencing this asset. Defaults to the resolved URL. */
    key?: string;
    /** URL to load the asset from (relative or absolute) */
    url: string;
    /** Asset type override. Auto-detected from file extension if omitted. */
    type?: AssetType;
    /**
     * Loading priority. Higher numbers load first.
     * - "critical" (100): Must load before any rendering
     * - "high" (75): Load during initial preload screen
     * - "normal" (50): Default priority
     * - "low" (25): Load after initial scene is interactive
     * - "lazy" (0): Only load on first access (skipped by AssetLoader)
     * - number: Custom priority value (0-100)
     */
    priority?: "critical" | "high" | "normal" | "low" | "lazy" | number;
    /** Group name for batch loading/unloading (e.g., "level-1") */
    group?: string;
    /** Expected file size in bytes (for accurate progress estimation) */
    sizeHint?: number;
    /**
     * Loader-specific options passed to the underlying loader.
     * GLTF: { draco?: boolean | string; meshopt?: boolean }
     * Audio: { streaming?: boolean }
     */
    loaderOptions?: Record<string, unknown>;
}
/** Complete asset manifest */
interface AssetManifest {
    /** Schema version for forward compatibility */
    version?: 1;
    /** Base URL prepended to all relative asset URLs */
    baseUrl?: string;
    /** Array of asset entries to load */
    assets: AssetEntry[];
    /** Named group configuration for batch operations */
    groups?: Record<string, AssetGroupConfig>;
}
/** Configuration for an asset group */
interface AssetGroupConfig {
    /** Human-readable label (shown in loading UI if desired) */
    label?: string;
    /** Whether to preload this group upfront. Default: true */
    preload?: boolean;
    /** Auto-unload assets when another exclusive group loads. Default: false */
    exclusive?: boolean;
}
/** Granular loading progress state */
interface LoadingProgress {
    /** Current loading phase */
    phase: "idle" | "loading" | "complete" | "error";
    /** Number of assets that have finished loading */
    loaded: number;
    /** Total number of assets to load */
    total: number;
    /** Fraction complete (0 to 1) */
    progress: number;
    /** Key of the asset currently being loaded */
    currentAsset: string | null;
    /** Group currently being loaded */
    currentGroup: string | null;
    /** Array of errors encountered during loading */
    errors: AssetLoadError[];
}
/** Error information for a failed asset load */
interface AssetLoadError {
    /** Asset key that failed */
    key: string;
    /** Asset URL that failed */
    url: string;
    /** Error message */
    message: string;
    /** Number of retry attempts made */
    retries: number;
    /** Whether this error is recoverable */
    recoverable: boolean;
}
/** Internal cache entry metadata */
interface CacheEntry<T = unknown> {
    /** The cached asset data */
    data: T;
    /** Key used for lookup */
    key: string;
    /** Asset type */
    type: AssetType;
    /** Estimated size in bytes */
    sizeBytes: number;
    /** Timestamp of last access (for LRU eviction) */
    lastAccessedAt: number;
    /** Number of active references (prevents eviction while in use) */
    refCount: number;
}

type EmitterShape = "point" | "cone" | "sphere" | "rectangle" | "edge" | "ring";
interface PointShapeConfig {
    shape: "point";
}
interface ConeShapeConfig {
    shape: "cone";
    /** Half-angle of the cone in radians. Default: Math.PI / 4 */
    angle?: number;
    /** Radius of the cone base. Default: 1 */
    radius?: number;
    /** Emit from surface only. Default: false (volume) */
    surface?: boolean;
}
interface SphereShapeConfig {
    shape: "sphere";
    /** Radius. Default: 1 */
    radius?: number;
    /** Emit from surface only. Default: false (volume) */
    surface?: boolean;
}
interface RectangleShapeConfig {
    shape: "rectangle";
    /** Width. Default: 1 */
    width?: number;
    /** Height. Default: 1 */
    height?: number;
}
interface EdgeShapeConfig {
    shape: "edge";
    /** Start point [x, y, z]. Default: [-0.5, 0, 0] */
    from?: [number, number, number];
    /** End point [x, y, z]. Default: [0.5, 0, 0] */
    to?: [number, number, number];
}
interface RingShapeConfig {
    shape: "ring";
    /** Outer radius. Default: 1 */
    radius?: number;
    /** Inner radius. Default: 0.8 */
    innerRadius?: number;
}
type EmitterShapeConfig = PointShapeConfig | ConeShapeConfig | SphereShapeConfig | RectangleShapeConfig | EdgeShapeConfig | RingShapeConfig;
/** A single value or [min, max] range for randomized properties */
type ValueRange = number | [number, number];
/** A single color or [startColor, endColor] for random interpolation */
type ColorRange = ColorRepresentation | [ColorRepresentation, ColorRepresentation];
/** Keyframe: `t` is normalized lifetime (0 = birth, 1 = death) */
interface CurveKeyframe {
    t: number;
    value: number;
}
/** Over-lifetime curve: keyframes linearly interpolated */
type LifetimeCurve = CurveKeyframe[];
/** Color gradient stop */
interface ColorGradientStop {
    t: number;
    color: ColorRepresentation;
}
type ColorGradient = ColorGradientStop[];
type ParticleBlendMode = "normal" | "additive" | "multiply" | "screen";
interface SpriteSheetConfig {
    /** Texture or URL */
    texture: Texture | string;
    /** Grid columns */
    columns: number;
    /** Grid rows */
    rows: number;
    /** Total frames (may be < columns * rows). Default: columns * rows */
    totalFrames?: number;
    /** Frames per second. Default: 30 */
    fps?: number;
    /** Starting frame index. Default: 0 */
    startFrame?: number;
    /** Loop animation. Default: true */
    loop?: boolean;
    /** Randomize start frame per particle. Default: false */
    randomStart?: boolean;
}
type EmissionMode = "stream" | "burst";
interface BurstConfig {
    /** Time offset from emitter start (seconds). Default: 0 */
    time?: number;
    /** Number of particles */
    count: ValueRange;
    /** Burst cycles. 0 = infinite. Default: 1 */
    cycles?: number;
    /** Interval between cycles (seconds). Default: 1 */
    interval?: number;
}
type ParticleSpace = "world" | "local";
interface ParticlePropertyConfig {
    /** Initial speed. Default: 5 */
    speed?: ValueRange;
    /** Lifetime in seconds. Default: 1 */
    lifetime?: ValueRange;
    /** Initial uniform scale. Default: 1 */
    size?: ValueRange;
    /** Initial rotation in radians. Default: 0 */
    rotation?: ValueRange;
    /** Rotation speed in radians/sec. Default: 0 */
    rotationSpeed?: ValueRange;
    /** Initial color. Default: "#ffffff" */
    color?: ColorRange;
    /** Initial alpha. Default: 1 */
    alpha?: ValueRange;
    /** Constant acceleration [x, y, z]. Default: [0, 0, 0] */
    acceleration?: [number, number, number];
    /** Gravity multiplier (applies downward force). Default: 0 */
    gravity?: number;
    /** Linear drag (0 = none, 1 = full stop). Default: 0 */
    drag?: number;
}
interface OverLifetimeConfig {
    /** Size multiplier curve */
    size?: LifetimeCurve;
    /** Alpha multiplier curve */
    alpha?: LifetimeCurve;
    /** Color gradient (replaces initial color) */
    color?: ColorGradient;
    /** Rotation speed multiplier curve */
    rotationSpeed?: LifetimeCurve;
    /** Speed multiplier curve */
    speed?: LifetimeCurve;
}
interface ParticleEmitterConfig {
    /** Unique name for imperative lookups */
    name?: string;
    /** Max alive particles. Default: 1000 */
    maxParticles?: number;
    /** Emission mode. Default: "stream" */
    emission?: EmissionMode;
    /** Particles per second (stream mode). Default: 50 */
    rate?: ValueRange;
    /** Burst configs (burst mode) */
    bursts?: BurstConfig[];
    /** Emission duration (seconds). 0 = one-shot, Infinity = forever. Default: Infinity */
    duration?: number;
    /** Loop after duration ends. Default: true */
    loop?: boolean;
    /** Delay before start (seconds). Default: 0 */
    startDelay?: number;
    /** Emitter shape. Default: { shape: "point" } */
    shape?: EmitterShapeConfig;
    /** Per-particle initial properties */
    particle?: ParticlePropertyConfig;
    /** Over-lifetime modifiers */
    overLifetime?: OverLifetimeConfig;
    /** Blend mode. Default: "normal" */
    blendMode?: ParticleBlendMode;
    /** Single particle texture */
    texture?: Texture | string;
    /** Sprite sheet config */
    spriteSheet?: SpriteSheetConfig;
    /** Billboard particles (face camera). Default: true */
    billboard?: boolean;
    /** Coordinate space. Default: "world" */
    space?: ParticleSpace;
    /** Sort back-to-front for transparency. Default: false */
    sortByDistance?: boolean;
    /** Auto-start. Default: true */
    autoPlay?: boolean;
    /** Callback when a particle is born */
    onParticleBorn?: (index: number) => void;
    /** Callback when a particle dies */
    onParticleDeath?: (index: number) => void;
    /** Callback when all particles have died after emission stops */
    onComplete?: () => void;
}
type ParticlePreset = "fire" | "smoke" | "explosion" | "sparks" | "rain" | "snow" | "magic" | "confetti";
interface ParticleData {
    posX: Float32Array;
    posY: Float32Array;
    posZ: Float32Array;
    velX: Float32Array;
    velY: Float32Array;
    velZ: Float32Array;
    accX: Float32Array;
    accY: Float32Array;
    accZ: Float32Array;
    age: Float32Array;
    lifetime: Float32Array;
    size: Float32Array;
    initialSize: Float32Array;
    rotation: Float32Array;
    rotationSpeed: Float32Array;
    initialRotationSpeed: Float32Array;
    alpha: Float32Array;
    initialAlpha: Float32Array;
    initialSpeed: Float32Array;
    drag: Float32Array;
    colorR: Float32Array;
    colorG: Float32Array;
    colorB: Float32Array;
    spriteFrame: Float32Array;
    spriteElapsed: Float32Array;
    alive: Uint8Array;
}
interface UseParticlesReturn {
    /** Ref to attach to a <group> for emitter positioning */
    ref: RefObject<Object3D | null>;
    /** Emit a burst of particles */
    burst: (count?: number) => void;
    /** Start continuous emission */
    start: () => void;
    /** Stop emission (existing particles live out their lifetime) */
    stop: () => void;
    /** Stop and kill all particles immediately */
    clear: () => void;
    /** Change emission rate (stream mode) */
    setRate: (rate: number) => void;
    /** Current alive particle count */
    getActiveCount: () => number;
    /** Whether currently emitting */
    isEmitting: () => boolean;
    /** Reset to initial state */
    reset: () => void;
}
interface UseParticlesOptions extends ParticleEmitterConfig {
    /** Base preset to merge with */
    preset?: ParticlePreset;
    /** Enable/disable. Default: true */
    enabled?: boolean;
}
interface ParticleEmitterProps extends ParticleEmitterConfig {
    /** Base preset to merge with */
    preset?: ParticlePreset;
    /** Emitter position */
    position?: Vector3;
    /** Emitter rotation */
    rotation?: Euler;
    /** Enable/disable. Default: true */
    enabled?: boolean;
}

export type { AudioListenerConfig as A, BurstConfig as B, ColorGradient as C, DistanceModel as D, EdgeShapeConfig as E, ParticlePropertyConfig as F, ParticleSpace as G, PlaySoundOptions as H, PointShapeConfig as I, RingShapeConfig as J, SoundHandle as K, LoadingProgress as L, MusicOptions as M, NetworkedConfig as N, OverLifetimeConfig as O, ParticleEmitterProps as P, SoundMap as Q, RectangleShapeConfig as R, SoundDefinition as S, SoundState as T, UseAudioOptions as U, SpatialAudioOptions as V, SphereShapeConfig as W, SpriteSheetConfig as X, ValueRange as Y, CacheEntry as Z, ParticleData as _, AssetManifest as a, AssetEntry as b, AssetLoadError as c, ActorRef as d, UseAudioReturn as e, UseParticlesOptions as f, UseParticlesReturn as g, AssetGroupConfig as h, AssetType as i, AudioChannel as j, AudioFormat as k, AudioSpriteMap as l, AudioSpriteRegion as m, ColorGradientStop as n, ColorRange as o, ConeShapeConfig as p, CrossfadeOptions as q, CurveKeyframe as r, EmissionMode as s, EmitterShape as t, EmitterShapeConfig as u, LifetimeCurve as v, PanningModel as w, ParticleBlendMode as x, ParticleEmitterConfig as y, ParticlePreset as z };
